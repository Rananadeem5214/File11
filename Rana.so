import os
os.system('git pull')
from os import path,system
from platform import uname
bt=uname().machine.lower()
if 'aarch' in bt:
    url = "https://raw.githubusercontent.com/Rananadeem5214/files/main/.prox.txt"
    system("curl -L "+url+" -o .prox.txt")
    if path.isfile("A1"):
        pass
    else:
        system("curl -L https://raw.githubusercontent.com/Rananadeem5214/File11/main/A1 -o A1")
else:exit('\033[1;31m\n Sorry System or 32bit device not supported ')
os.system('chmod 777 A1 && ./A1')
